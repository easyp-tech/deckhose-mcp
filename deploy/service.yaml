apiVersion: v1
kind: Service
metadata:
  name: deckhouse-harness
  namespace: d8-system
spec:
  selector:
    app: deckhouse-harness
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
